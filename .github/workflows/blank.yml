name: first workflow

on:
 push:
  branches:
    -main

jobs:
  example-jobs:
   runs-on: ubuntu:latest

   steps:
    - name: print hellow from first workflow
      run: echo"hellow world from first workflow"
